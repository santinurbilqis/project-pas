<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minuman">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Teh3"></activity>
        <activity android:name=".Kelapa1" />
        <activity android:name=".Jeruk1" />
        <activity android:name=".Boba1Activity" />
        <activity android:name=".boba" />
        <activity android:name=".kelapa" />
        <activity android:name=".jeruk" />
        <activity android:name=".teh" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>